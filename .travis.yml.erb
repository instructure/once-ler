language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.3
gemfile:
<% Dir["gemfiles/*gemfile"].each do |file| %>
  - <%= file %>
<% end %>
matrix:
  allow_failures:
  <% Dir["gemfiles/*ar_edge.gemfile"].each do |file| -%>
    - gemfile: <%= file %>
  <% end -%>
bundler_args: --without development
